# name: Demo 03 - releases, possibly smierc
# description: Spin zwalnia fluent loaded, wiec potem shoot moze, ale nie musi, zabic.

fluents loaded, alive
actions spin, shoot, load
initially alive
spin releases loaded if true
shoot causes !alive if loaded
impossible load if loaded
load causes loaded if !loaded
